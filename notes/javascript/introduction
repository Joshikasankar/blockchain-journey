 ECMA Script?
   Created by Brendan Eich at Netscape.
   Standardization process by Ecma International.
      -syntax.
      -semantics.
      -features.

(ES5,ES6)
-------------------------------------------------------------------------
java script :
      Java Script makes the website intractive.
     Logic of the website initially created to make web page come alive.

How it started : 
   Run in browser (client side).
How its going :
   *web browser.
   *mobile dev.
   *server side.
   *desktop apps.

Javascript Engine :
   * reads the script.
   * convert the script to machine code.
   * runs, pretty fast.
------------------------------------------------------------------
in javascript comments :
   * Single line comment : // ---->comment it the single line 
     Shortcut key -----> (ctrl + /).select whole line and comment.
   * Multi Line comment; /* */ -----> comment the multiple lines.
     Shortcyt key ----->(shift + alt + a).select multiple line and comment.
---------------------------------------------------------------------------
DataTypes :
     string
     Number
    Boolean
    undifined

 to find the type of data :  ----------> typeof _data----> eg : typeof 18 -->'number'

alert();
it executes in orderwise eg : 
          if i decleare a script in html page and new new external in new page
          first i will put <script src = "sript.js"></script>--->it executes first.
          then remaining.
