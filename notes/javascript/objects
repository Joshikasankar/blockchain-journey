Objects :
      it is also a data type.
      it act as key and value pairs.
      in objects keywords are also used as keys.
      if the key has a space it mast be given in string.
      objects can be created without class.
declaring a object :
          // objects 
         let a  = {};
         console.log(typeof a);  ------> output : object
// object :
  let food = {
        fruits : "apple, mango , strawberry,banana",
        veggi : "carrot,beatroot,cabbage",
        price : 2000
       };
  console.log(food);

// extracting a particular value from the object
let food = {
        fruits : "apple,mango , strawberry,banana",
        veggi : "carrot,beatroot,cabbage",
        price : 2000
  };
console.log(food.veggi);
--------------------------------------------------------------------
In objects variables are allowed for values in objects :
 Example :
    let rawveggi = "carrot,beans,sweetpotato";
 let food = {
        fruits : "apple,mango , strawberry,banana",
        veggi : rawveggi+",beatroot,cabbage",
        price : 2000
     };
       console.log(food.veggi);
----------------------------------------------------------------------------
if the key has a space it mast be given in string.

         let rawveggi = "carrot,beans,sweetpotato";
         let food = {
         fruits : "apple,mango , strawberry,banana",
         "veggi" : rawveggi+",beatroot,cabbage",
         price : 2000
          };
         console.log(food["veggi"]);
---------------------------------------------------------------
Deleting a element in object :
Syntax :
    delete object_name.key;

let rawveggi = "carrot,beans,sweetpotato";
 let food = {
        fruits : "apple,mango , strawberry,banana",
        "veggi" : rawveggi+",beatroot,cabbage",
        price : 2000
     };
       console.log(food["veggi"]);

       delete food.price;
       console.log(food);
-------------------------------------------------------------------------------
// adding a new element to the object :
 syntax :
     object_name[key] = value
Example :
     food['protin'] = 'egg,meat,panner';
        console.log(food);
-----------------------------------------------------------------------------
// updating the element
              food.price = 3000;
              console.log(food);
------------------------------------------------------------------------------
// adding function to the object
     this = > refer to current object.
     let rawveggi = "carrot,beans,sweetpotato";
 let food = {
        fruits : "apple,mango , strawberry,banana",
        "veggi" : rawveggi+",beatroot,cabbage",
        price : 2000,

        // defining a function
         priceadd(price){
            return this.price +=2;
         }
     };
       console.log(food["veggi"]);

       console.log(food);
        food.price = 3000;
        console.log(food);
        console.log(food.priceadd(11))--> 11 is default value.
-----------------------------------------------------------------------------------
                              IN OPERATOR
 
It check the properties in the object. the given property is there.
it returns the boolean type 
    let names ={
        name : "joshika",
        age :20,
        department : "IT"
};
console.log("name" in names);
---------------Another Example---------------------------------
let food = {
       fruits : ("apple"),
       veggis :("carrot"),
       nuts : ("cashew")
};

console.log(food.fruits);
 food.fruits = "strawberry"; 
console.log(food);
delete food.nuts;
console.log(food);
// in operator
console.log("fruits" in food);
console.log("nuts" in food);
--------------------------------------------------------------------------------
                                For loop
it print the key values 60
let names ={
        name : "joshika",
        age :20,
        department : "IT"
};
 names.initial = "R S";
console.log(names);

for(key in names){
        console.log(names[key]);
}
let food = {
       fruits : ("apple"),
       veggis :("carrot"),
       nuts : ("cashew")
};
for(key in food){
        console.log(key , '-',food[key]);
}
--------------------------------------------------------------------------------
object by reference
          

