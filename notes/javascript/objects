Objects :
      it is also a data type.
      it act as key and value pairs.
      in objects keywords are also used as keys.
      if the key has a space it mast be given in string.
declaring a object :
          // objects 
         let a  = {};
         console.log(typeof a);  ------> output : object
// object :
let food = {
        fruits : "apple,mango , strawberry,banana",
        veggi : "carrot,beatroot,cabbage",
        price : 2000
};
console.log(food);

// extracting a particular value from the object
let food = {
        fruits : "apple,mango , strawberry,banana",
        veggi : "carrot,beatroot,cabbage",
        price : 2000
  };
console.log(food.veggi);
--------------------------------------------------------------------
In objects variables are allowed for values in objects :
 Example :
    let rawveggi = "carrot,beans,sweetpotato";
 let food = {
        fruits : "apple,mango , strawberry,banana",
        veggi : rawveggi+",beatroot,cabbage",
        price : 2000
     };
       console.log(food.veggi);
----------------------------------------------------------------------------
if the key has a space it mast be given in string.

         let rawveggi = "carrot,beans,sweetpotato";
         let food = {
         fruits : "apple,mango , strawberry,banana",
         "veggi" : rawveggi+",beatroot,cabbage",
         price : 2000
          };
         console.log(food["veggi"]);
---------------------------------------------------------------
Deleting a element in object :
Syntax :
    delete object_name.key;

let rawveggi = "carrot,beans,sweetpotato";
 let food = {
        fruits : "apple,mango , strawberry,banana",
        "veggi" : rawveggi+",beatroot,cabbage",
        price : 2000
     };
       console.log(food["veggi"]);

       delete food.price;
       console.log(food);
-------------------------------------------------------------------------------
// adding a new element to the object :
 syntax :
     object_name[key] = value
Example :
     food['protin'] = 'egg,meat,panner';
        console.log(food);
-----------------------------------------------------------------------------
// updating the element
              food.price = 3000;
              console.log(food);
------------------------------------------------------------------------------
// adding function to the object
     this = > refer to current object.
     








